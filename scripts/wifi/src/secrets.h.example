#pragma once

// Copy this file to `secrets.h` (same directory) and fill in your network.
// `secrets.h` is gitignored — your password never leaves this machine.

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
